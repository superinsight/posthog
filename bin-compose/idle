#!/bin/bash

echo "Container starting..."

# Infinite loop to keep container alive
while true; do
    echo "$(date): Container is running - heartbeat message"
    sleep 60  # Wait 60 seconds between messages
done
